# Completion Format

Use this final shape:

```text
Task:
Commands Run:
Changed Paths:
Verification:
Important Output:
Rollback Notes:
Residual Risk:
```

If no command was run, write `Commands Run: none`.
If verification could not run, state the exact blocker.
