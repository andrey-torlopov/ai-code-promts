# Root Cause Format

Use this structure:

```text
Symptom:
Evidence:
Root cause:
Fix plan:
Verification:
Residual risk:
```

If root cause is not proven, say `Most likely cause` and list the missing evidence.
