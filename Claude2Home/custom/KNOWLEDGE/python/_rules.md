# Python Rules

1. Inspect `pyproject.toml`, lockfiles and test config before choosing commands.
2. Prefer project-local virtual environments or configured tooling.
3. Do not install dependencies without confirmation.
4. Keep scripts deterministic and avoid hidden network calls.
5. Use structured parsers for structured data when available.
