# Очередь работ

Будущие шаги здесь **не нумеруются** и заранее не создаются. Шаг появляется только по запросу
пользователя: план — `../Steps/Step-XX.md`, результат — `../Steps/Step-XX-result.md`, строка — в
`../steps.md`.

Этот файл держит кандидатов: что имеет смысл сделать следующим и почему. Порядок — предложение, а не
обязательство.

Заказчик этот файл не читает. Публичная проекция очереди — блок «Рекомендуемая
очередность» в `../steps.md`: верхние кандидаты пересказываются там короткими
самодостаточными пунктами, без `Q-NN` и без ссылок сюда. `Q-NN` и прочие внутренние ID
живут только внутри `Context/`.

**Правило ранжирования:** {{FILL_RANKING_RULE}}.

## Кандидаты

| Приоритет | Кандидат | Почему сейчас или потом | Что откроет |
|---:|---|---|---|
| 1 | {{FILL_CANDIDATE}} | {{FILL_RATIONALE}} | {{FILL_GATE}} |

## Блокирующие вопросы к пользователю

Вопрос попадает сюда, если решить его самому нельзя, и он меняет состав или порядок работ. Пока
ответа нет — принимается умолчание, чтобы работа не стояла, и оно помечается допущением.

| ID | Вопрос | Почему нельзя решить самому | Умолчание |
|---|---|---|---|
| Q-01 | {{FILL_QUESTION_OR_NONE}} | {{FILL_WHY_BLOCKED_OR_NONE}} | {{FILL_DEFAULT_OR_NONE}} |
