# env — внешнее окружение задачи

## Источники знаний

| Тип | Путь | Категории задачи |
|---|---|---|
| knowledge | {{KB_PATH}} | {{KB_CATEGORIES}} |

Путь — абсолютный или относительный от корня папки задачи. «Категории задачи» — какие
разделы базы релевантны этой задаче; они же — категории по умолчанию при экспорте.
`—` — фильтра нет, релевантна вся база.

База — плоский набор атомарных самодостаточных `F-NN`/`H-NN` плюс реестр `README.md`.
Записи цитировать в файлах задачи текстом с датой (`база F-12 (player), снимок
YYYY-MM-DD`), не Markdown-ссылкой. Экспорт в базу и удаление из неё — только по явному
запросу пользователя.
